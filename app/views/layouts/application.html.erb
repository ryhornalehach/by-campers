<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width">
    <title>BY-Campers</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <link href="https://fonts.googleapis.com/css?family=Raleway" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  </head>

  <div class="navbar-fixed">
    <nav class="transparent z-depth-0 black-text">
      <div class="nav-wrapper">
        <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
        <ul class="right hide-on-med-and-down compress">
          <% if current_user %>
            <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
            <li><%= link_to "Profile", root_path %> </li>
            <% if current_user.admin %>
              <li><%= link_to "All Users List", root_path %></li>
            <% else %>
              <li><%= link_to "My clients", root_path %> </li>
            <% end %>
          <% else %>
            <li><%=link_to "Sign In", new_user_session_path %></li>
            <li><%=link_to "Sign Up", new_user_registration_path %></li>
          <% end %>
        </ul>
        <ul class="side-nav flow-text" id="mobile-demo">
          <% if current_user %>
            <% if current_user.admin %>
              <li><%= link_to "All Users List", root_path %></li>
            <% else %>
              <li><%= link_to "My clients", root_path %> </li>
            <% end %>
            <li><%= link_to "Profile", root_path %> </li>
            <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
          <% else %>
            <li><%=link_to "Sign In", new_user_session_path %></li>
            <li><%=link_to "Sign Up", new_user_registration_path %></li>
          <% end %>
        </ul>
      </div>
    </nav>
  </div>



  <!-- <section class="video-container2">
    <img src="<%= asset_path('campervan.jpg')%>">
    <div class="callout">
      <div>
        <p>Object-Fit is the CSS Background-Size: Cover for Inline Images &amp; Videos.
        <br>It's not Microsoft friendly.</p>
      </div>
    </div>
  </section> -->



  <body>

    <%= yield %>
  </body>
  <script>
    $('.button-collapse').sideNav();
  </script>
</html>
